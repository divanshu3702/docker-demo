pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/divanshu3702/docker-demo.git'
            }
        }

        stage('Build') {
            steps {
                sh 'echo Build Started'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t my-app .'
            }
        }
    }
}'
            }
        }

        stage('Build') {
            steps {
                sh 'echo Build Started'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t my-app .'
            }
        }
    }
}'
            }
        }

        stage('Build') {
            steps {
                sh 'echo Build Started'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t my-app .'
            }
        }
    }
}
